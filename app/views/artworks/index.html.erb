<html>
  <body>
    <% @artworks.each do |artwork| %>
    <p>Title: <%= artwork.title %></p>
    <p>Artist: <%= artwork.artist %></p>
    <%= link_to "Google Map for #{artwork.location}", "http://www.google.com/maps?q=#{artwork.location}" %>

    <% end %>
  </body>
</html>
